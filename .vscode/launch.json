{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Gin API",
      "type": "go",
      "request": "launch",
      "mode": "auto", // "debug"でもOK
      "program": "${workspaceFolder}/main.go", // エントリーポイント
      "env": {
        "GIN_MODE": "debug"
      }
    }
  ]
}
