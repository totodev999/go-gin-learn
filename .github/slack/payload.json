{
  "text": "${{ steps.test.outcome == 'failure' || steps.lint.outcome == 'failure' && ':x:' || ':white_check_mark:' }} *Go CI – ${{ github.repository }}*  (<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|Run #${{ github.run_number }}>) on `${{ github.ref_name }}`",
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "*Test*: `${{ steps.test.outcome }}`  |  *Lint*: `${{ steps.lint.outcome }}`"
      }
    }
    {{#if (or (eq steps.test.outcome "failure") (eq steps.lint.outcome "failure"))}}
    ,
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "```${{ steps.logs.outputs.body }}```"
      }
    }
    {{/if}}
  ]
}